{
  "name": "jp-blood-donation",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "predev": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
  }
}
